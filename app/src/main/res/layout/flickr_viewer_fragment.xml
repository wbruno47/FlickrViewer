<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity$PlaceholderFragment">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:textAppearanceLarge"
        android:layout_alignParentTop="true"
        android:id="@+id/flickr_viewer_title"
        android:paddingLeft="@dimen/side_margin"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:background="#99000000"
        android:textColor="@android:color/white" />

    <GridView
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:clipToPadding="true"

        android:scrollbarStyle="outsideOverlay"
        android:id="@+id/photo_grid_view"

        android:listSelector="@drawable/image_holder_selector"
        android:drawSelectorOnTop="true"
        android:columnWidth="@dimen/column_width"
        android:numColumns="auto_fit"
        android:stretchMode="spacingWidthUniform"
        android:verticalSpacing="@dimen/grid_view_spacing"
        android:horizontalSpacing="@dimen/grid_view_spacing"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/flickr_viewer_title" />

    <Button
        android:id="@+id/footer_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/button_footer"
        android:background="@drawable/footer_button"
        android:visibility="gone"
        />
</RelativeLayout>
